# Стенды фичи v0.3.0 — обзор инструментов Claude

Начат 2026-09-26. Решения и процесс — `.planning/ROADMAP.md`, раздел «v0.3.0 — обзор инструментов Claude»;
спека и задачи — `specs/001-claude-tooling-view/`.

## Правило

Стенд на каждый новый компонент, как в редизайне (`.planning/REDESIGN-TRAIL.md`), но **без блока «Сейчас»** —
компонента ещё нет. Стенд = части с вариантами + киоск (`<ссылка>#kiosk`) со сборкой выбранного и сегментом
«Состояние». Данные — настоящие с этой машины. Порядок: стенд → «Выбор: …» пользователя → запись сюда →
реализация → проверка пользователем → коммит. Язык Trail и решения шагов 1–6 редизайна не пересматриваются.

Папки стендов — `.planning/sketches/1NN-<компонент>/`, генератор — по образцу `006-dialogs/gen6.cjs`.

## Журнал

| # | Компонент | Стенд | Выбор | Коммит |
|---|-----------|-------|-------|--------|
| 1 | Раскладка вкладки (T027): панель областей, заголовок, фильтр типов, строка, поиск, пусто/загрузка/ошибка | https://claude.ai/artifact/7Q1ntncbDJv6UY3tzhL1hh · `.planning/sketches/101-tools-layout/stand.html` (генератор `gen101.cjs`, данные `scan-raw.json`) | Панель областей A · Заголовок C · Фильтр типов B · Строка B · Поиск C · Пусто и ошибки A | — |
| 2 | Детали элемента (T030): где детали, шапка, файл, текст, MCP, плагин, ошибка/удалён | https://claude.ai/artifact/CSXYZtYH6WUuL26sd8QT1n · `.planning/sketches/102-tool-detail/stand.html` (генератор `gen102.cjs`) | Где детали A · Шапка B · Файл B · Текст A · MCP B · Плагин C · Ошибка A | — |
| 3 | Итог проекта (T034): источник, перекрытия, только проектное, заголовок проекта | https://claude.ai/artifact/CT51Hq3maM2JKV2ncodorJ · `.planning/sketches/103-project-view/stand.html` (генератор `gen103.cjs`) | Источник B · Перекрытия C · Только проектное C · Заголовок проекта C | — |
| 4 | Переключатель (T040) | — | — | — |

## Стенд 1 — раскладка вкладки (выбор 2026-09-26)

Стенд `.planning/sketches/101-tools-layout/stand.template.html` (генератор `gen101.cjs`).

- **Панель областей A, «как проекты».** Карточка 28 слева (как панель проектов Conversations): заголовок
  «All **scopes**» 22 light/bold, Global — первая капсула 44 со значком глобуса и чипом общего числа, подпись
  «Projects · N» 12, проекты — капсулы 44 со значком папки; выбранная — заливка цветом текста, текст фона bold;
  удалённые с диска — тихо (имя 60 %) с чипом «missing».
- **Заголовок C, «плитки».** Имя области 22 light/bold, под ним пять плиток 20 на `--raised` (паддинг 10 12):
  число 22 light, подпись 11 — Skills, Plugins, MCP, Commands, Agents.
- **Фильтр типов B, «чипы со значком».** Капсулы 32 на `--raised`: значок типа 14, название 13, число 11 bold;
  выбранная — `accent-soft` с кольцом 1.5 акцентом, значок акцентом. Первая — All.
- **Строка B, «две строки».** Строка 60, скругление 20: круг 36 на `--raised` со значком типа, имя 14 bold,
  описание 12 тихо под ним в одну строку; справа чипы 22 (версия плагина моно, имя плагина, «off», «plugin off»,
  «error», режим `skillOverrides`). Выключенный — имя и значок тихо; недоступный — вся строка 55 %;
  выбранная — заливка цветом текста.
- **Поиск C, «в строке фильтра».** Капсула 40 шириной 220 справа в строке с чипами фильтра, на `--ground` с тенью
  внутрь; при вводе — чип «N of M» и круг-крестик; совпадения подсвечены `accent-soft` с кольцом.
- **Пусто и ошибки A.** Текст 13 по центру: загрузка — пульсирующая точка акцента; пустой поиск — «No tools
  match «…»» и «Clear search» акцентом; источник не прочитался — плашка 16 `danger-soft` с путём моно над
  списком, остальное показывается.
- **Улучшения стенда (решение пользователя).** Плашка «Выбор» — строка выделяется целиком и кнопка
  «Копировать»; вместо копирования ссылки киоска — «Открыть киоск» отдельным окном; состояние в киоске меняет
  только сегмент «Состояние» (без автопереключения). Записано в `/redesign-stand` и в базу `001-shell`.

## Стенд 2 — детали элемента (выбор 2026-09-26)

Стенд `.planning/sketches/102-tool-detail/stand.template.html` (генератор `gen102.cjs`, общие куски — из `gen101.cjs`).

- **Где детали A, «третья карточка».** Карточка справа (≈40 % правой части, не уже 360), список сужается и
  прячет плитки; крестик и Esc закрывают детали.
- **Шапка B, «крупно и строкой».** Имя 28 light/bold (последнее слово bold), строка 12 «Skill · user · off ·
  плагин», описание 13 в три строки; справа капсула 32 «Show in Explorer» и круг 40 «закрыть».
- **Файл B, «чипы».** Капсулы 32 в ряд: имя файла со значком папки (клик — Explorer), источник (`~/.claude`,
  проект или место объявления MCP), плагин; полный путь — в подсказке.
- **Текст A, «прямо на карточке».** Markdown без front matter на фоне карточки: h1 22 light, h2 17 bold,
  h3 15 bold, код — блоки 16 на `--ground`, ссылки акцентом.
- **MCP B, «командная строка».** Команда и аргументы целиком в утопленном блоке моно 12 на `--ground` с кругом
  Copy; ниже строки Transport (чип) и Environment/Headers: ключ моно, значение-маска «••••», клик раскрывает
  одно значение до смены элемента.
- **Плагин C, «плитки и список».** Пять плиток состава (Skills, Commands, Agents, MCP, Hooks), строки Version,
  Marketplace, Installed/updated, Folder; ниже группы элементов плагина — первые 6 и «and N more».
- **Ошибка A.** Файл не прочитался — плашка `danger-soft` с текстом моно и «Try again»; элемент удалён, пока
  детали открыты, — текст 13 по центру и «Back to list».

### Замеры US1 (T033, dev-сборка, 2026-09-26)

- SC-001: скан Global через IPC — 91 мс (холодный, debug), переключение на вкладку до строк — 40 мс.
- SC-003: ввод в поиск на 203 элементах — 18–71 мс на символ вместе с задержкой CDP.

## Стенд 3 — итог проекта (выбор 2026-09-26)

Стенд `.planning/sketches/103-project-view/stand.template.html` (генератор `gen103.cjs`, данные — итог Breezee).

- **Источник B, «только своё».** В области проекта у элементов `project` и `local` — чип 22 `accent-soft`
  «project» / «local»; глобальные и плагинные без метки источника (у плагинных — чип плагина, как раньше).
- **Перекрытия C, «вложенно».** Перекрытый элемент (MCP, agent) — компактная строка 44 (капсула) с отступом 48
  сразу под победителем, прозрачность 70 %, перед описанием «overridden ·» цветом warn; круг значка 28.
  Одинаковые имена skills/commands — чип warn «same name».
- **Только проектное C, «тумблер в заголовке».** Тумблер 36×22 с подписью «Project only» справа от имени
  проекта; включённый — дорожка акцентом, ручка цветом `accent-ink`. Только в области проекта.
- **Заголовок проекта C, «чипы сводки».** Над плитками чипы 24: «N from this project» (`accent-soft`),
  «N override» (warn, если есть), «N Claude sees in total».
